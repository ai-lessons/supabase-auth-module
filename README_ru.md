# Supabase Auth Module v2.0.0

Полнофункциональный, готовый к продакшену модуль аутентификации Supabase с Express.js сервером, управлением пользователями, системой регистрационных пар, административным интерфейсом и React компонентами.

## 🔍 Проверка безопасности

**✅ БЕЗОПАСНОСТЬ ПОДТВЕРЖДЕНА**: В репозитории отсутствуют секретные ключи, пароли или токены. Все чувствительные данные хранятся только в `.env.example` как заполнители.

## 🚀 Что нового в v2.0.0

### Полный набор функций из оригинального WordPress плагина:

- **✅ Управление системными пользователями** - Создание и управление пользователями в отдельной таблице Supabase
- **✅ Система регистрационных пар** - Сопоставление страниц регистрации и благодарственных страниц (полная логика из оригинального плагина)
- **✅ Административная панель** - Полноценный веб-интерфейс для управления парами и пользователями
- **✅ Улучшенная безопасность** - CSRF защита, ограничение запросов, валидация входных данных (все меры безопасности из оригинала)
- **✅ React компоненты** - Готовый к использованию компонент AuthForm для интеграции с фронтендом
- **✅ Аудит логирование** - Комплексное отслеживание событий аутентификации
- **✅ Готовность к продакшену** - Все функции оригинального WordPress плагина без зависимости от WordPress

## 📋 Описание модуля

### 1) Технические особенности:

- **Express.js сервер** с middleware аутентификации и административным интерфейсом
- **JWT верификация** с кэшированием JWKS (10 минут)
- **Универсальный Supabase клиент** для серверных операций
- **Управление системными пользователями** в отдельной таблице Supabase
- **Система регистрационных пар** с полными CRUD операциями
- **Веб-интерфейс администратора** для удобного управления
- **Улучшенная безопасность** с CSRF защитой, ограничением запросов и валидацией входных данных
- **React компоненты** для интеграции с фронтендом
- **Аудит логирование** для мониторинга безопасности

### 2) Функциональность:

**Серверная часть (`server/`):**
- `index.js` - основной роутер с callback для Supabase и административными маршрутами
- `verifyJwt.js` - безопасная валидация токенов с кэшированием JWKS
- `middleware/authMiddleware.js` - защита маршрутов с Bearer token валидацией
- `middleware/securityMiddleware.js` - CSRF защита, ограничение запросов, валидация входных данных

**Клиентская часть (`client/`):**
- `AuthForm.jsx` - React компонент для форм аутентификации

**База данных (`database/`):**
- `schema.sql` - полная схема базы данных со всеми таблицами

**Утилиты:**
- Встроенный Supabase клиент с отключенной сессией для серверных операций

### 3) Ключевые маршруты:
- `POST /api/auth/supabase-callback` - обработка аутентификации, создание пользователей и логирование регистраций
- `GET /api/auth/test-protected` - тестовый защищенный маршрут
- `GET /api/auth/health` - health check endpoint
- `GET /api/auth/admin/dashboard` - веб-интерфейс администратора
- `GET/POST/PUT/DELETE /api/auth/admin/pairs` - API для управления регистрационными парами
- `GET /api/auth/admin/users` - управление системными пользователями

### 4) Безопасность (все меры из оригинального плагина):
- **JWKS кэширование** для производительности (10 минут)
- **Bearer token валидация** в middleware
- **CSRF защита** с валидацией Origin/Referer
- **Ограничение запросов** для аутентификационных endpoints
- **Валидация входных данных** для email, UUID, URL путей
- **Security headers** (CSP, X-Frame-Options и др.)
- **Аудит логирование** всех событий аутентификации

### 5) Схема базы данных:

**system_users** - Аккаунты системных пользователей (заменяет WordPress пользователей):
- `supabase_user_id` - Ссылка на пользователя Supabase auth
- `email` - Email пользователя
- `username` - Имя пользователя
- `display_name` - Отображаемое имя
- `role` - Роль пользователя (user, admin и т.д.)
- `last_login` - Время последнего входа

**wp_registration_pairs** - Регистрационные пары (из оригинального плагина):
- `site_url` - URL сайта
- `registration_page_url` - URL страницы регистрации
- `thankyou_page_url` - URL благодарственной страницы
- `registration_page_id` - Внешний ID страницы
- `thankyou_page_id` - Внешний ID страницы

**wp_user_registrations** - Логирование регистраций (из оригинального плагина):
- `user_email` - Email пользователя
- `user_id` - ID пользователя Supabase
- `pair_id` - Ссылка на регистрационную пару
- `registration_url` - URL, где пользователь зарегистрировался
- `thankyou_page_url` - URL благодарственной страницы

**auth_events** - Логирование событий аутентификации:
- `user_id` - ID пользователя
- `event_type` - Тип события (login, logout, registration)
- `ip_address` - IP адрес клиента
- `user_agent` - User agent клиента

## 🚀 Пошаговое руководство по интеграции

### Шаг 1: Установка модуля

```bash
# Через npm (рекомендуется)
npm install supabase-auth-module

# Или через git
npm install github:ai-lessons/supabase-auth-module
```

### Шаг 2: Настройка окружения

Создайте файл `.env` в корне вашего проекта:

```env
# Конфигурация Supabase
SUPABASE_URL=ваш_supabase_project_url
SUPABASE_KEY=ваш_supabase_anon_key
SUPABASE_SERVICE_KEY=ваш_supabase_service_role_key

# Конфигурация сервера
PORT=3001
NODE_ENV=development

# Конфигурация приложения
SITE_URL=ваш_application_url
```

### Шаг 3: Настройка базы данных

Выполните SQL скрипт в вашей базе данных Supabase:

```sql
-- Используйте полную схему из database/schema.sql
-- Это создаст все необходимые таблицы:
-- system_users, wp_registration_pairs, wp_user_registrations, auth_events
```

### Шаг 4: Интеграция в Express.js приложение

```javascript
const express = require('express');
const supabaseAuth = require('supabase-auth-module');

const app = express();

// Middleware для парсинга JSON
app.use(express.json());

// Подключение модуля аутентификации
app.use('/api/auth', supabaseAuth);

// Ваши обычные маршруты
app.get('/', (req, res) => {
  res.json({ message: 'Добро пожаловать в API' });
});

app.listen(3001, () => {
  console.log('Сервер запущен на порту 3001');
});
```

### Шаг 5: Интеграция с фронтендом через React компонент

```javascript
import AuthForm from 'supabase-auth-module/client/AuthForm';

// В вашем React компоненте
function LoginPage() {
  return (
    <div>
      <h1>Добро пожаловать в наше приложение</h1>
      <AuthForm
        supabaseUrl={process.env.NEXT_PUBLIC_SUPABASE_URL}
        supabaseAnonKey={process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY}
        onSuccess={(userData) => {
          console.log('Пользователь аутентифицирован:', userData);
          // Обработка успешной аутентификации
        }}
        onError={(error) => {
          console.error('Ошибка аутентификации:', error);
          // Обработка ошибок аутентификации
        }}
      />
    </div>
  );
}
```

### Шаг 6: Защита маршрутов

```javascript
const { authMiddleware } = require('supabase-auth-module/server/middleware/authMiddleware');

// Защита отдельных маршрутов
app.get('/api/user/profile', authMiddleware, (req, res) => {
  res.json({
    message: 'Доступ к защищенному профилю разрешен',
    user: req.user
  });
});

// Защита административных маршрутов
app.get('/api/admin/*', authMiddleware, (req, res, next) => {
  // Дополнительная проверка роли администратора при необходимости
  if (req.user.role !== 'admin') {
    return res.status(403).json({ error: 'Требуется доступ администратора' });
  }
  next();
});
```

### Шаг 7: Доступ к административному интерфейсу

Доступ к административной панели по адресу:
```
GET /api/auth/admin/dashboard
```

Административный интерфейс предоставляет:
- Управление регистрационными парами (CRUD операции)
- Обзор системных пользователей
- Просмотр логов регистраций
- Удобное управление конфигурацией

## 🔧 Система регистрационных пар

Система регистрационных пар позволяет сопоставлять определенные страницы регистрации с соответствующими благодарственными страницами, точно как в оригинальном WordPress плагине.

### Создание регистрационных пар:

```javascript
// Через API
fetch('/api/auth/admin/pairs', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ВАШ_JWT_ТОКЕН'
  },
  body: JSON.stringify({
    site_url: 'https://вашсайт.com',
    registration_page_url: '/services/register',
    thankyou_page_url: '/services/thank-you',
    registration_page_id: 'services-register',
    thankyou_page_id: 'services-thankyou'
  })
});

// Или через административную панель по адресу /api/auth/admin/dashboard
```

### Как работают регистрационные пары:

1. Пользователь регистрируется на `/services/register`
2. Система находит соответствующую регистрационную пару
3. Пользователь перенаправляется на `/services/thank-you`
4. Регистрация логируется с контекстом пары для аналитики

## 🤖 Промпты для VS Code AI расширений

### Промпт 1: Полная интеграция модуля

```
Мне нужно интегрировать supabase-auth-module v2.0.0 в мой Express.js проект.

Требования:
1. Добавить маршруты аутентификации по /api/auth
2. Настроить переменные окружения для Supabase
3. Настроить таблицы базы данных используя предоставленную схему
4. Создать защищенные маршруты с authMiddleware
5. Интегрировать React компонент AuthForm
6. Настроить систему регистрационных пар
7. Настроить доступ к административной панели

Использовать модуль: npm install supabase-auth-module

Предоставить полный код интеграции с объяснениями.
```

### Промпт 2: Управление регистрационными парами

```
Реализовать управление регистрационными парами в моем приложении используя supabase-auth-module:

Требования:
1. Создать API endpoints для управления регистрационными парами
2. Интегрировать административную панель для визуального управления
3. Обрабатывать редиректы регистраций на основе пар
4. Логировать регистрации с контекстом пар
5. Добавить валидацию и обработку ошибок

Использовать: административные маршруты и React компоненты supabase-auth-module.
```

### Промпт 3: React фронтенд с регистрационными парами

```
Создать React приложение с интеграцией supabase-auth-module:

1. Несколько страниц регистрации с разными благодарственными страницами
2. Компонент AuthForm на каждой странице регистрации
3. Динамические редиректы на основе регистрационных пар
4. Управление сессиями пользователей
5. Административный интерфейс для управления парами

Использовать: @supabase/supabase-js и supabase-auth-module/client/AuthForm
```

### Промпт 4: Реализация безопасности

```
Реализовать комплексную безопасность для моей системы аутентификации:

1. CSRF защита для всех endpoints
2. Ограничение запросов для маршрутов аутентификации
3. Валидация всех пользовательских входных данных
4. Конфигурация security headers
5. Аудит логирование для событий безопасности

Использовать: security middleware и функции валидации supabase-auth-module.
```

## 🐛 Решение проблем

### Частые проблемы:

1. **Ошибка "Invalid token"**
   - Проверьте SUPABASE_URL в переменных окружения
   - Убедитесь, что токен не истек
   - Проверьте настройки JWT в Supabase Dashboard

2. **Ошибка "Database insert error"**
   - Проверьте существование всех необходимых таблиц
   - Убедитесь, что SUPABASE_SERVICE_KEY корректен
   - Проверьте права доступа к базе данных и RLS политики

3. **"CSRF protection failed"**
   - Убедитесь, что запросы включают правильные Origin/Referer headers
   - Проверьте, что переменная окружения SITE_URL соответствует вашему домену
   - Проверьте, приходит ли запрос с того же origin

4. **"Rate limit exceeded"**
   - Подождите, пока окно ограничения сбросится
   - Реализуйте exponential backoff в вашем клиенте
   - Рассмотрите увеличение лимитов для высоконагруженных приложений

5. **Административная панель не загружается**
   - Убедитесь, что пользователь имеет валидный JWT токен
   - Проверьте, имеет ли пользователь соответствующие права
   - Убедитесь, что все необходимые таблицы созданы

6. **Регистрационные пары не работают**
   - Убедитесь, что URL регистрационных пар точно совпадают
   - Проверьте, правильно ли пара сохранена в базе данных
   - Убедитесь, что URL регистрации захватывается из referer

## 🔒 Лучшие практики безопасности

### Переменные окружения:
- Никогда не коммитьте `.env` файлы в систему контроля версий
- Используйте разные ключи для разработки и продакшена
- Регулярно меняйте SUPABASE_SERVICE_KEY

### Безопасность базы данных:
- Включите RLS политики на таблицах Supabase
- Используйте service role key только для серверных операций
- Реализуйте правильную индексацию для производительности

### Безопасность приложения:
- Всегда используйте HTTPS в продакшене
- Реализуйте правильную конфигурацию CORS
- Регулярно обновляйте зависимости
- Мониторьте аудит логи на предмет подозрительной активности

## 📞 Поддержка

- Создавайте GitHub issues для багов и вопросов: [GitHub Repository](https://github.com/ai-lessons/supabase-auth-module)
- Проверяйте документацию Supabase для общих вопросов по аутентификации
- Используйте промпты выше для быстрой интеграции с AI ассистентами

## 📄 Лицензия

MIT License - смотрите файл [LICENSE](LICENSE) для деталей.

---

**Версия**: 2.0.0  
**Последнее обновление**: Ноябрь 2025  
**Совместимость**: Node.js >= 16.0.0, Express.js 4.x, React 16.8+
